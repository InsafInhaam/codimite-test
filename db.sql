-- Question 2 - Task 1:

SELECT CustomerName FROM Customers  JOIN FROM Orders WHERE OrderAmount

SELECT CustomerID WHERE OrderAmount 

SELECT CustomerID FROM Orders WHERE OrderAmount AND 

-- not sure with the sql we could get the customer id and orders where sum of the OrderAmount and then like with another sql query or we can find the CustomerName where CustomerID from the above query. and create them to a new Table. 